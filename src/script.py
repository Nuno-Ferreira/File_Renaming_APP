"""Logic to rename the files in the directory."""

from pathlib import Path

def rename_files(directory: str, new_name: str) -> None:
    """Rename the files in the directory."""
    print(f"Renaming files in {directory} to {new_name}")
    pass